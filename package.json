{
  "name": "util.wait",
  "version": "0.0.28",
  "author": "jmquigley <jmquigley@outlook.com>",
  "description": "Javascript pause/wait functions",
  "main": "./index.js",
  "types": "./index.d.ts",
  "private": false,
  "debug": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jmquigley/util.wait"
  },
  "bugs": {
    "url": "https://github.com/jmquigley/util.wait/issues"
  },
  "engines": {
    "node": ">=7.x"
  },
  "devDependencies": {
    "@types/node": "^8.0.53",
    "ava": "^0.24.0",
    "coveralls": "^3.0.0",
    "nyc": "^11.3.0",
    "tslint": "^5.8.0",
    "typescript": "^2.6.2"
  },
  "pkgwrap": {
    "cleanup": [
      "docs"
    ]
  },
  "scripts": {
    "all": "yarn install && yarn run build && yarn run docs && yarn test && yarn run report",
    "build": "yarn run clean && yarn run lint && pkgwrap --build",
    "clean": "pkgwrap --clean",
    "coverage": "pkgwrap --coverage",
    "docs": "pkgwrap --docs",
    "lint": "pkgwrap --lint",
    "preinstall": "yarn global add util.pkgwrap",
    "prepublishOnly": "yarn run all",
    "report": "pkgwrap --reporting",
    "test": "pkgwrap --testing --ava"
  }
}
